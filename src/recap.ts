const username: string | number = 'ijaz';

const suma = (a: number, b: number) => {
  return a + b;
}

suma(1, 3);

class Person {
  constructor(age: number, lastName: string) {};
}

const jaz = new Person(30, 'Lino');
